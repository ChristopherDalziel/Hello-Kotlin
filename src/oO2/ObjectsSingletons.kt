package oO2

// A singleton is a class of which there can only ever be one object be defined from it

// An object can only have a single instance - This is called an object declaration
object Cache {
    val name = "HyperCache"

    fun retrieveData(): Int {
        return 0
    }
}
